/node_modules
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo
.DS_Store
npm-debug.log*
.env*
!.env.example

# verification builds (npm run build:check) — never shares .next with a live dev server
.next-check/
